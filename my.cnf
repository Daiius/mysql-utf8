[mysqld]
performance_schema=off
character-set-server=utf8mb4

innodb_buffer_pool_size = 64M
max_connections = 100  # あまりに少ないと本番環境がエラーに
innodb_log_buffer_size = 256K

[client]
default-character-set=utf8mb4

